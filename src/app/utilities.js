// export const filterPlans = (match, filteredPlans) => {
//   const filtered = []
//   let match = type === 'number' ? Number(value) : value
//   if(match) {
//     filtered.concat(filteredPlans.filter((plan) => plan[key] === match))
//   }

//   return
// }